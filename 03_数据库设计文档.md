# 旅游攻略制作网站 - 数据库设计文档

## 1. 文档概述

### 1.1 文档目的
本文档详细描述系统数据库表结构、字段定义、索引设计、关系约束等，为数据库建表和开发提供依据。

### 1.2 适用范围
- 数据库表结构设计
- 索引优化策略
- 数据字典定义
- ER关系图

### 1.3 数据库选型
- **数据库**: MySQL 8.0+
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci
- **存储引擎**: InnoDB

## 2. 数据库命名规范

### 2.1 命名规则
- 表名: `t_模块名_业务含义`，小写，下划线分隔
- 字段名: 小写，下划线分隔
- 索引名: `idx_字段名` 或 `uk_字段名`(唯一索引)
- 主键: 统一使用 `id`，bigint类型，自增

### 2.2 通用字段
所有表包含以下通用字段:
- `create_time`: 创建时间
- `update_time`: 更新时间
- `is_deleted`: 逻辑删除标识(0-未删除, 1-已删除)

## 3. 概念模型设计(ER图)

```
┌──────────────┐         ┌──────────────────┐
│   t_user     │1       *│  t_itinerary     │
│              │─────────│                  │
│ 用户表        │         │ 攻略主表          │
└──────────────┘         └──────────────────┘
                                    │1
                                    │
                                    │*
                         ┌──────────────────┐
                         │ t_itinerary_day  │
                         │                  │
                         │ 每日行程表        │
                         └──────────────────┘
                                    │1
                                    │
                                    │*
                         ┌──────────────────┐
                         │t_itinerary_spot  │
                         │                  │
                         │ 景点时间表        │
                         └──────────────────┘
```

## 4. 物理表结构设计

### 4.1 用户模块

#### 4.1.1 用户表 (t_user)

**表说明**: 存储系统用户基本信息

| 字段名 | 数据类型 | 允许NULL | 默认值 | 说明 |
|--------|---------|---------|--------|------|
| id | BIGINT | NO | AUTO_INCREMENT | 主键ID |
| username | VARCHAR(50) | NO | - | 用户名(唯一) |
| password | VARCHAR(100) | NO | - | 密码(BCrypt加密) |
| email | VARCHAR(100) | YES | NULL | 邮箱 |
| phone | VARCHAR(20) | YES | NULL | 手机号 |
| nickname | VARCHAR(50) | YES | NULL | 昵称 |
| avatar | VARCHAR(255) | YES | NULL | 头像URL |
| gender | TINYINT | YES | 0 | 性别: 0-未知, 1-男, 2-女 |
| birthday | DATE | YES | NULL | 生日 |
| signature | VARCHAR(200) | YES | NULL | 个性签名 |
| status | TINYINT | NO | 1 | 状态: 0-禁用, 1-正常 |
| create_time | DATETIME | NO | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| is_deleted | TINYINT | NO | 0 | 逻辑删除: 0-未删除, 1-已删除 |

**索引设计**:
```sql
PRIMARY KEY (id)
UNIQUE KEY uk_username (username)
KEY idx_email (email)
KEY idx_phone (phone)
```

**建表SQL**:
```sql
CREATE TABLE `t_user` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `password` VARCHAR(100) NOT NULL COMMENT '密码(BCrypt加密)',
  `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `nickname` VARCHAR(50) DEFAULT NULL COMMENT '昵称',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `gender` TINYINT DEFAULT 0 COMMENT '性别: 0-未知, 1-男, 2-女',
  `birthday` DATE DEFAULT NULL COMMENT '生日',
  `signature` VARCHAR(200) DEFAULT NULL COMMENT '个性签名',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用, 1-正常',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除: 0-未删除, 1-已删除',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  KEY `idx_email` (`email`),
  KEY `idx_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

### 4.2 攻略模块

#### 4.2.1 攻略主表 (t_itinerary)

**表说明**: 存储攻略的基本信息和整体内容

| 字段名 | 数据类型 | 允许NULL | 默认值 | 说明 |
|--------|---------|---------|--------|------|
| id | BIGINT | NO | AUTO_INCREMENT | 主键ID |
| user_id | BIGINT | NO | - | 用户ID |
| title | VARCHAR(200) | NO | - | 攻略标题 |
| destination | VARCHAR(100) | NO | - | 目的地 |
| days | INT | NO | - | 旅行天数 |
| start_date | DATE | NO | - | 开始日期 |
| end_date | DATE | NO | - | 结束日期 |
| budget | DECIMAL(10,2) | YES | NULL | 总预算 |
| ticket_budget | DECIMAL(10,2) | YES | NULL | 门票预算 |
| transport_budget | DECIMAL(10,2) | YES | NULL | 交通预算 |
| food_budget | DECIMAL(10,2) | YES | NULL | 美食预算 |
| style | VARCHAR(50) | YES | NULL | 旅行风格(休闲/紧凑/文化/美食等) |
| html_content | LONGTEXT | YES | NULL | HTML格式内容 |
| json_data | LONGTEXT | YES | NULL | JSON格式数据(用于编辑) |
| cover_image | VARCHAR(255) | YES | NULL | 封面图片URL |
| view_count | INT | NO | 0 | 浏览次数 |
| like_count | INT | NO | 0 | 点赞次数 |
| share_count | INT | NO | 0 | 分享次数 |
| status | TINYINT | NO | 0 | 状态: 0-草稿, 1-已发布, 2-已删除 |
| version | INT | NO | 1 | 版本号 |
| publish_time | DATETIME | YES | NULL | 发布时间 |
| create_time | DATETIME | NO | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| is_deleted | TINYINT | NO | 0 | 逻辑删除: 0-未删除, 1-已删除 |

**索引设计**:
```sql
PRIMARY KEY (id)
KEY idx_user_id (user_id)
KEY idx_destination (destination)
KEY idx_start_date (start_date)
KEY idx_status (status)
KEY idx_create_time (create_time)
```

**建表SQL**:
```sql
CREATE TABLE `t_itinerary` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `title` VARCHAR(200) NOT NULL COMMENT '攻略标题',
  `destination` VARCHAR(100) NOT NULL COMMENT '目的地',
  `days` INT NOT NULL COMMENT '旅行天数',
  `start_date` DATE NOT NULL COMMENT '开始日期',
  `end_date` DATE NOT NULL COMMENT '结束日期',
  `budget` DECIMAL(10,2) DEFAULT NULL COMMENT '总预算',
  `ticket_budget` DECIMAL(10,2) DEFAULT NULL COMMENT '门票预算',
  `transport_budget` DECIMAL(10,2) DEFAULT NULL COMMENT '交通预算',
  `food_budget` DECIMAL(10,2) DEFAULT NULL COMMENT '美食预算',
  `style` VARCHAR(50) DEFAULT NULL COMMENT '旅行风格',
  `html_content` LONGTEXT COMMENT 'HTML格式内容',
  `json_data` LONGTEXT COMMENT 'JSON格式数据(用于编辑)',
  `cover_image` VARCHAR(255) DEFAULT NULL COMMENT '封面图片URL',
  `view_count` INT NOT NULL DEFAULT 0 COMMENT '浏览次数',
  `like_count` INT NOT NULL DEFAULT 0 COMMENT '点赞次数',
  `share_count` INT NOT NULL DEFAULT 0 COMMENT '分享次数',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '状态: 0-草稿, 1-已发布, 2-已删除',
  `version` INT NOT NULL DEFAULT 1 COMMENT '版本号',
  `publish_time` DATETIME DEFAULT NULL COMMENT '发布时间',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除: 0-未删除, 1-已删除',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_destination` (`destination`),
  KEY `idx_start_date` (`start_date`),
  KEY `idx_status` (`status`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='攻略主表';
```

#### 4.2.2 每日行程表 (t_itinerary_day)

**表说明**: 存储攻略中每一天的行程安排

| 字段名 | 数据类型 | 允许NULL | 默认值 | 说明 |
|--------|---------|---------|--------|------|
| id | BIGINT | NO | AUTO_INCREMENT | 主键ID |
| itinerary_id | BIGINT | NO | - | 攻略ID |
| day_number | INT | NO | - | 第几天(从1开始) |
| date | DATE | NO | - | 具体日期 |
| departure_time | TIME | YES | NULL | 出发时间 |
| theme | VARCHAR(100) | YES | NULL | 当日主题 |
| weather_condition | VARCHAR(50) | YES | NULL | 天气状况 |
| weather_temp_min | DECIMAL(5,2) | YES | NULL | 最低温度 |
| weather_temp_max | DECIMAL(5,2) | YES | NULL | 最高温度 |
| sort_order | INT | NO | 0 | 排序号 |
| create_time | DATETIME | NO | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| is_deleted | TINYINT | NO | 0 | 逻辑删除: 0-未删除, 1-已删除 |

**索引设计**:
```sql
PRIMARY KEY (id)
KEY idx_itinerary_id (itinerary_id)
KEY idx_date (date)
UNIQUE KEY uk_itinerary_day (itinerary_id, day_number)
```

**建表SQL**:
```sql
CREATE TABLE `t_itinerary_day` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `itinerary_id` BIGINT NOT NULL COMMENT '攻略ID',
  `day_number` INT NOT NULL COMMENT '第几天(从1开始)',
  `date` DATE NOT NULL COMMENT '具体日期',
  `departure_time` TIME DEFAULT NULL COMMENT '出发时间',
  `theme` VARCHAR(100) DEFAULT NULL COMMENT '当日主题',
  `weather_condition` VARCHAR(50) DEFAULT NULL COMMENT '天气状况',
  `weather_temp_min` DECIMAL(5,2) DEFAULT NULL COMMENT '最低温度',
  `weather_temp_max` DECIMAL(5,2) DEFAULT NULL COMMENT '最高温度',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序号',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除: 0-未删除, 1-已删除',
  PRIMARY KEY (`id`),
  KEY `idx_itinerary_id` (`itinerary_id`),
  KEY `idx_date` (`date`),
  UNIQUE KEY `uk_itinerary_day` (`itinerary_id`, `day_number`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='每日行程表';
```

#### 4.2.3 景点时间表 (t_itinerary_spot)

**表说明**: 存储每日行程中的具体景点、活动和空闲时间安排

| 字段名 | 数据类型 | 允许NULL | 默认值 | 说明 |
|--------|---------|---------|--------|------|
| id | BIGINT | NO | AUTO_INCREMENT | 主键ID |
| day_id | BIGINT | NO | - | 所属日程ID |
| title | VARCHAR(200) | NO | - | 标题/名称 |
| type | VARCHAR(20) | NO | 'spot' | 类型: spot-景点, food-餐饮, traffic-交通, free-空闲时间 ⭐新增 |
| content | TEXT | YES | NULL | 详细内容描述 |
| start_time | TIME | NO | - | 开始时间 |
| end_time | TIME | NO | - | 结束时间 |
| duration | INT | YES | NULL | 持续时间(分钟) |
| location | VARCHAR(200) | YES | NULL | 地点/地址 |
| latitude | DECIMAL(10,6) | YES | NULL | 纬度 |
| longitude | DECIMAL(10,6) | YES | NULL | 经度 |
| cost | DECIMAL(10,2) | YES | NULL | 费用 |
| need_booking | TINYINT | NO | 0 | 是否需要预约: 0-否, 1-是 |
| booking_advance_days | INT | YES | NULL | 需提前预约天数 |
| ticket_required | TINYINT | NO | 1 | 是否需要门票: 0-否, 1-是 |
| transport_from_prev | VARCHAR(50) | YES | NULL | 从上一地点的交通方式 |
| transport_duration | INT | YES | NULL | 交通时长(分钟) |
| notes | VARCHAR(500) | YES | NULL | 备注说明 |
| **free_time_type** | **VARCHAR(20)** | **YES** | **NULL** | **⭐空闲时间类型: activity-自由活动, rest-休息调整, flexible-机动时间** |
| **free_time_notes** | **VARCHAR(500)** | **YES** | **NULL** | **⭐空闲时间备注说明** |
| sort_order | INT | NO | 0 | 排序号 |
| create_time | DATETIME | NO | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| is_deleted | TINYINT | NO | 0 | 逻辑删除: 0-未删除, 1-已删除 |

**索引设计**:
```sql
PRIMARY KEY (id)
KEY idx_day_id (day_id)
KEY idx_type (type)  -- ⭐新增索引
```

**建表SQL**:
```sql
CREATE TABLE `t_itinerary_spot` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `day_id` BIGINT NOT NULL COMMENT '所属日程ID',
  `title` VARCHAR(200) NOT NULL COMMENT '标题/名称',
  `type` VARCHAR(20) NOT NULL DEFAULT 'spot' COMMENT '类型: spot-景点, food-餐饮, traffic-交通, free-空闲时间',
  `content` TEXT COMMENT '详细内容描述',
  `start_time` TIME NOT NULL COMMENT '开始时间',
  `end_time` TIME NOT NULL COMMENT '结束时间',
  `duration` INT DEFAULT NULL COMMENT '持续时间(分钟)',
  `location` VARCHAR(200) DEFAULT NULL COMMENT '地点/地址',
  `latitude` DECIMAL(10,6) DEFAULT NULL COMMENT '纬度',
  `longitude` DECIMAL(10,6) DEFAULT NULL COMMENT '经度',
  `cost` DECIMAL(10,2) DEFAULT NULL COMMENT '费用',
  `need_booking` TINYINT NOT NULL DEFAULT 0 COMMENT '是否需要预约: 0-否, 1-是',
  `booking_advance_days` INT DEFAULT NULL COMMENT '需提前预约天数',
  `ticket_required` TINYINT NOT NULL DEFAULT 1 COMMENT '是否需要门票: 0-否, 1-是',
  `transport_from_prev` VARCHAR(50) DEFAULT NULL COMMENT '从上一地点的交通方式',
  `transport_duration` INT DEFAULT NULL COMMENT '交通时长(分钟)',
  `notes` VARCHAR(500) DEFAULT NULL COMMENT '备注说明',
  `free_time_type` VARCHAR(20) DEFAULT NULL COMMENT '空闲时间类型: activity-自由活动, rest-休息调整, flexible-机动时间',
  `free_time_notes` VARCHAR(500) DEFAULT NULL COMMENT '空闲时间备注说明',
  `sort_order` INT NOT NULL DEFAULT 0 COMMENT '排序号',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除: 0-未删除, 1-已删除',
  PRIMARY KEY (`id`),
  KEY `idx_day_id` (`day_id`),
  KEY `idx_type` (`type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='景点时间表';
```

---

### 4.3 智能偏好分析模块 ⭐新增

#### 4.3.1 关键词规则表 (t_keyword_rule)

**表说明**: 存储关键词到维度的映射规则，支持动态配置

| 字段名 | 数据类型 | 允许NULL | 默认值 | 说明 |
|--------|---------|---------|--------|------|
| id | BIGINT | NO | AUTO_INCREMENT | 主键ID |
| keyword | VARCHAR(50) | NO | - | 关键词 |
| dimension | VARCHAR(20) | NO | - | 维度名称 |
| score | INT | NO | 50 | 分数贡献(0-100) |
| status | TINYINT | NO | 1 | 状态: 0-禁用, 1-启用 |
| create_time | DATETIME | NO | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引设计**:
```sql
PRIMARY KEY (id)
UNIQUE KEY uk_keyword_dimension (keyword, dimension)
KEY idx_dimension (dimension)
```

**建表SQL**:
```sql
CREATE TABLE `t_keyword_rule` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `keyword` VARCHAR(50) NOT NULL COMMENT '关键词',
  `dimension` VARCHAR(20) NOT NULL COMMENT '维度名称',
  `score` INT NOT NULL DEFAULT 50 COMMENT '分数贡献(0-100)',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-禁用, 1-启用',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_keyword_dimension` (`keyword`, `dimension`),
  KEY `idx_dimension` (`dimension`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='关键词规则表';

-- 初始化数据
INSERT INTO `t_keyword_rule` (`keyword`, `dimension`, `score`) VALUES
('美食', '美食指数', 90),
('好吃', '美食指数', 85),
('餐厅', '美食指数', 80),
('小吃', '美食指数', 75),
('博物馆', '文化深度', 95),
('历史', '文化深度', 90),
('文化', '文化深度', 85),
('古迹', '文化深度', 90),
('自然', '自然风光', 90),
('风景', '自然风光', 85),
('山水', '自然风光', 90),
('轻松', '休闲程度', 90),
('休闲', '休闲程度', 85),
('慢', '休闲程度', 80),
('孩子', '亲子友好', 95),
('亲子', '亲子友好', 90),
('家庭', '亲子友好', 85),
('拍照', '摄影需求', 90),
('打卡', '摄影需求', 85),
('网红', '摄影需求', 80),
('购物', '购物倾向', 90),
('商场', '购物倾向', 85),
('特产', '购物倾向', 75),
('探险', '探险精神', 95),
('刺激', '探险精神', 90),
('冒险', '探险精神', 90);
```

---

### 4.4 攻略主表扩展 ⭐新增

为了支持智能偏好分析，需要在攻略主表中添加相关字段：

```sql
ALTER TABLE `t_itinerary` 
ADD COLUMN `preference_description` VARCHAR(500) DEFAULT NULL COMMENT '用户偏好描述(原始输入)' AFTER `style`,
ADD COLUMN `food_index` INT DEFAULT 50 COMMENT '美食指数权重(0-100)' AFTER `preference_description`,
ADD COLUMN `culture_depth` INT DEFAULT 50 COMMENT '文化深度权重(0-100)' AFTER `food_index`,
ADD COLUMN `nature_scenery` INT DEFAULT 50 COMMENT '自然风光权重(0-100)' AFTER `culture_depth`,
ADD COLUMN `leisure_level` INT DEFAULT 50 COMMENT '休闲程度权重(0-100)' AFTER `nature_scenery`,
ADD COLUMN `parent_child` INT DEFAULT 30 COMMENT '亲子友好权重(0-100)' AFTER `leisure_level`,
ADD COLUMN `photography` INT DEFAULT 50 COMMENT '摄影需求权重(0-100)' AFTER `parent_child`,
ADD COLUMN `shopping` INT DEFAULT 40 COMMENT '购物倾向权重(0-100)' AFTER `photography`,
ADD COLUMN `adventure` INT DEFAULT 30 COMMENT '探险精神权重(0-100)' AFTER `shopping`;
```

#### 4.2.4 攻略标签表 (t_itinerary_tag)

**表说明**: 存储攻略的标签信息

| 字段名 | 数据类型 | 允许NULL | 默认值 | 说明 |
|--------|---------|---------|--------|------|
| id | BIGINT | NO | AUTO_INCREMENT | 主键ID |
| itinerary_id | BIGINT | NO | - | 攻略ID |
| tag_name | VARCHAR(50) | NO | - | 标签名称 |
| create_time | DATETIME | NO | CURRENT_TIMESTAMP | 创建时间 |

**索引设计**:
```sql
PRIMARY KEY (id)
KEY idx_itinerary_id (itinerary_id)
KEY idx_tag_name (tag_name)
```

**建表SQL**:
```sql
CREATE TABLE `t_itinerary_tag` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `itinerary_id` BIGINT NOT NULL COMMENT '攻略ID',
  `tag_name` VARCHAR(50) NOT NULL COMMENT '标签名称',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_itinerary_id` (`itinerary_id`),
  KEY `idx_tag_name` (`tag_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='攻略标签表';
```

#### 4.2.5 攻略收藏表 (t_itinerary_favorite)

**表说明**: 存储用户收藏的攻略

| 字段名 | 数据类型 | 允许NULL | 默认值 | 说明 |
|--------|---------|---------|--------|------|
| id | BIGINT | NO | AUTO_INCREMENT | 主键ID |
| user_id | BIGINT | NO | - | 用户ID |
| itinerary_id | BIGINT | NO | - | 攻略ID |
| create_time | DATETIME | NO | CURRENT_TIMESTAMP | 收藏时间 |

**索引设计**:
```sql
PRIMARY KEY (id)
UNIQUE KEY uk_user_itinerary (user_id, itinerary_id)
KEY idx_itinerary_id (itinerary_id)
```

**建表SQL**:
```sql
CREATE TABLE `t_itinerary_favorite` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `itinerary_id` BIGINT NOT NULL COMMENT '攻略ID',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '收藏时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_itinerary` (`user_id`, `itinerary_id`),
  KEY `idx_itinerary_id` (`itinerary_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='攻略收藏表';
```

### 4.3 系统模块

#### 4.3.1 操作日志表 (t_operation_log)

**表说明**: 记录用户的关键操作日志

| 字段名 | 数据类型 | 允许NULL | 默认值 | 说明 |
|--------|---------|---------|--------|------|
| id | BIGINT | NO | AUTO_INCREMENT | 主键ID |
| user_id | BIGINT | YES | NULL | 用户ID |
| module | VARCHAR(50) | YES | NULL | 模块名称 |
| action | VARCHAR(50) | YES | NULL | 操作类型 |
| description | VARCHAR(500) | YES | NULL | 操作描述 |
| request_url | VARCHAR(500) | YES | NULL | 请求URL |
| request_method | VARCHAR(10) | YES | NULL | 请求方法 |
| request_params | TEXT | YES | NULL | 请求参数(JSON) |
| response_result | TEXT | YES | NULL | 响应结果 |
| ip_address | VARCHAR(50) | YES | NULL | IP地址 |
| user_agent | VARCHAR(500) | YES | NULL | 浏览器信息 |
| execution_time | INT | YES | NULL | 执行时长(ms) |
| status | TINYINT | NO | 1 | 状态: 0-失败, 1-成功 |
| error_msg | TEXT | YES | NULL | 错误信息 |
| create_time | DATETIME | NO | CURRENT_TIMESTAMP | 操作时间 |

**索引设计**:
```sql
PRIMARY KEY (id)
KEY idx_user_id (user_id)
KEY idx_module (module)
KEY idx_create_time (create_time)
```

**建表SQL**:
```sql
CREATE TABLE `t_operation_log` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `user_id` BIGINT DEFAULT NULL COMMENT '用户ID',
  `module` VARCHAR(50) DEFAULT NULL COMMENT '模块名称',
  `action` VARCHAR(50) DEFAULT NULL COMMENT '操作类型',
  `description` VARCHAR(500) DEFAULT NULL COMMENT '操作描述',
  `request_url` VARCHAR(500) DEFAULT NULL COMMENT '请求URL',
  `request_method` VARCHAR(10) DEFAULT NULL COMMENT '请求方法',
  `request_params` TEXT COMMENT '请求参数(JSON)',
  `response_result` TEXT COMMENT '响应结果',
  `ip_address` VARCHAR(50) DEFAULT NULL COMMENT 'IP地址',
  `user_agent` VARCHAR(500) DEFAULT NULL COMMENT '浏览器信息',
  `execution_time` INT DEFAULT NULL COMMENT '执行时长(ms)',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 0-失败, 1-成功',
  `error_msg` TEXT COMMENT '错误信息',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_module` (`module`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='操作日志表';
```

#### 4.3.2 系统配置表 (t_system_config)

**表说明**: 存储系统配置参数

| 字段名 | 数据类型 | 允许NULL | 默认值 | 说明 |
|--------|---------|---------|--------|------|
| id | BIGINT | NO | AUTO_INCREMENT | 主键ID |
| config_key | VARCHAR(100) | NO | - | 配置键 |
| config_value | TEXT | YES | NULL | 配置值 |
| description | VARCHAR(200) | YES | NULL | 配置描述 |
| create_time | DATETIME | NO | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引设计**:
```sql
PRIMARY KEY (id)
UNIQUE KEY uk_config_key (config_key)
```

**建表SQL**:
```sql
CREATE TABLE `t_system_config` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `config_key` VARCHAR(100) NOT NULL COMMENT '配置键',
  `config_value` TEXT COMMENT '配置值',
  `description` VARCHAR(200) DEFAULT NULL COMMENT '配置描述',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_config_key` (`config_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统配置表';
```

## 5. 数据字典

### 5.1 枚举值定义

#### 5.1.1 用户状态 (user_status)
| 值 | 说明 |
|----|------|
| 0 | 禁用 |
| 1 | 正常 |

#### 5.1.2 攻略状态 (itinerary_status)
| 值 | 说明 |
|----|------|
| 0 | 草稿 |
| 1 | 已发布 |
| 2 | 已删除 |

#### 5.1.3 旅行风格 (travel_style)
| 值 | 说明 |
|----|------|
| leisure | 休闲游 |
| intensive | 紧凑游 |
| culture | 文化游 |
| food | 美食游 |
| adventure | 探险游 |
| shopping | 购物游 |
| family | 亲子游 |
| romantic | 浪漫游 |

#### 5.1.4 交通方式 (transport_type)
| 值 | 说明 |
|----|------|
| walk | 步行 |
| bike | 自行车 |
| subway | 地铁 |
| bus | 公交 |
| taxi | 出租车 |
| car | 自驾车 |

#### 5.1.5 性别 (gender)
| 值 | 说明 |
|----|------|
| 0 | 未知 |
| 1 | 男 |
| 2 | 女 |

### 5.2 基础数据初始化

```sql
-- 初始化系统配置
INSERT INTO `t_system_config` (`config_key`, `config_value`, `description`) VALUES
('ai.model.name', 'qwen-max', 'AI模型名称'),
('ai.model.temperature', '0.7', 'AI生成温度'),
('ai.model.max_tokens', '4000', 'AI最大Token数'),
('weather.cache.hours', '1', '天气缓存时长(小时)'),
('itinerary.draft.auto_save.interval', '30000', '草稿自动保存间隔(ms)'),
('user.token.expiration', '1800', '用户Token过期时间(秒)');
```

## 6. 索引优化策略

### 6.1 索引设计原则
1. **主键索引**: 所有表必须有主键
2. **唯一索引**: 业务上唯一的字段建立唯一索引
3. **联合索引**: 遵循最左前缀原则
4. **覆盖索引**: 尽量让索引包含查询所需的所有字段
5. **避免过度索引**: 单表索引不超过5个

### 6.2 核心索引说明

| 表名 | 索引字段 | 索引类型 | 说明 |
|------|---------|---------|------|
| t_user | username | UNIQUE | 用户名唯一性校验 |
| t_user | email | NORMAL | 邮箱登录查询 |
| t_user | phone | NORMAL | 手机号登录查询 |
| t_itinerary | user_id | NORMAL | 查询用户攻略列表 |
| t_itinerary | destination | NORMAL | 按目的地筛选 |
| t_itinerary | start_date | NORMAL | 按日期范围查询 |
| t_itinerary | status | NORMAL | 按状态过滤 |
| t_itinerary_day | itinerary_id | NORMAL | 查询攻略的日程 |
| t_itinerary_day | itinerary_id, day_number | UNIQUE | 保证天数唯一性 |
| t_itinerary_spot | day_id | NORMAL | 查询日程的景点 |
| t_itinerary_favorite | user_id, itinerary_id | UNIQUE | 防止重复收藏 |

## 7. 分区策略(可选)

### 7.1 适用场景
当数据量超过千万级时，考虑对以下表进行分区:

### 7.2 分区方案

#### 7.2.1 t_operation_log 按月分区
```sql
ALTER TABLE `t_operation_log` 
PARTITION BY RANGE (YEAR(create_time) * 100 + MONTH(create_time)) (
    PARTITION p202401 VALUES LESS THAN (202402),
    PARTITION p202402 VALUES LESS THAN (202403),
    PARTITION p202403 VALUES LESS THAN (202404),
    ...
    PARTITION pmax VALUES LESS THAN MAXVALUE
);
```

#### 7.2.2 t_itinerary 按年分区
```sql
ALTER TABLE `t_itinerary` 
PARTITION BY RANGE (YEAR(start_date)) (
    PARTITION p2024 VALUES LESS THAN (2025),
    PARTITION p2025 VALUES LESS THAN (2026),
    PARTITION p2026 VALUES LESS THAN (2027),
    PARTITION pmax VALUES LESS THAN MAXVALUE
);
```

## 8. 数据安全设计

### 8.1 敏感数据加密
- **用户密码**: 使用BCrypt加密存储
- **手机号**: 可考虑AES加密存储
- **邮箱**: 可考虑AES加密存储

### 8.2 数据备份策略
- **全量备份**: 每周日凌晨2点
- **增量备份**: 每天凌晨2点
- **binlog日志**: 实时开启，保留7天

### 8.3 数据清理策略
- **操作日志**: 保留6个月
- **草稿攻略**: 保留30天
- **已删除数据**: 保留3个月后物理删除

## 9. 性能优化建议

### 9.1 查询优化
1. 避免SELECT *，只查询需要的字段
2. 使用LIMIT限制返回行数
3. 合理使用JOIN，避免多表关联
4. 大文本字段(html_content, json_data)单独查询

### 9.2 分页优化
```sql
-- 传统分页(数据量大时性能差)
SELECT * FROM t_itinerary WHERE user_id = 1 ORDER BY id LIMIT 10000, 10;

-- 优化分页(使用游标)
SELECT * FROM t_itinerary WHERE user_id = 1 AND id > 10000 ORDER BY id LIMIT 10;
```

### 9.3 读写分离
- **写操作**: 主库
- **读操作**: 从库(攻略列表、详情查询)

## 10. 扩展性设计

### 10.1 预留字段
为方便后续功能扩展，部分表预留字段:

```sql
-- t_user 表预留扩展字段
ALTER TABLE `t_user` 
ADD COLUMN `ext_field1` VARCHAR(255) DEFAULT NULL COMMENT '扩展字段1',
ADD COLUMN `ext_field2` VARCHAR(255) DEFAULT NULL COMMENT '扩展字段2';

-- t_itinerary 表预留扩展字段
ALTER TABLE `t_itinerary` 
ADD COLUMN `ext_field1` VARCHAR(255) DEFAULT NULL COMMENT '扩展字段1',
ADD COLUMN `ext_field2` VARCHAR(255) DEFAULT NULL COMMENT '扩展字段2';
```

### 10.2 JSON字段应用
对于结构可能变化的数据，使用JSON格式存储:
- `t_itinerary.json_data`: 存储攻略的完整结构化数据
- `t_operation_log.request_params`: 存储请求参数

## 11. 数据库监控

### 11.1 监控指标
- QPS (每秒查询数)
- TPS (每秒事务数)
- 慢查询数量
- 连接池使用情况
- 锁等待情况
- 主从延迟

### 11.2 慢查询阈值
- 设置为1秒
- 定期分析慢查询日志
- 针对性优化SQL

## 12. 附录

### 12.1 完整建表脚本
将所有建表SQL整合为一个脚本文件: `schema.sql`

### 12.2 测试数据脚本
创建测试数据的SQL脚本: `test_data.sql`

### 12.3 数据库变更管理
使用Flyway或Liquibase进行数据库版本管理




