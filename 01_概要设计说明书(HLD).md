# 旅游攻略制作网站 - 概要设计说明书(HLD)

## 1. 文档概述

### 1.1 项目背景
本项目是一个基于AI的智能化旅游攻略生成平台，用户输入旅行相关信息后，系统通过通义千问大模型自动生成个性化的旅游攻略，并支持用户对攻略进行可视化编辑和保存。

**核心创新点**:
- **智能偏好分析**: 用户通过自然语言描述旅行重心(如"我喜欢美食和博物馆")，AI自动分析8个维度权重并指导预算分配
- **灵活时间管理**: 支持自定义空闲时间段，标记为自由活动或时间结余

### 1.2 文档目的
本文档描述系统的整体架构设计、模块划分、技术选型及关键技术方案，为后续详细设计和开发提供指导。

### 1.3 适用范围
- 系统架构设计
- 模块功能划分
- 技术栈选择
- 数据流向设计
- 部署架构

## 2. 系统总体设计

### 2.1 系统定位
一个智能化的旅游攻略生成与编辑平台，结合AI技术和实时数据服务，为用户提供个性化、可定制的旅行方案。

### 2.2 核心功能
1. **智能偏好分析**: 用户自由输入旅行重心，AI分析8个维度权重(美食/文化/自然/休闲/亲子/摄影/购物/探险)
2. **智能攻略生成**: 基于用户偏好权重和预算分配，AI自动生成完整攻略
3. **可视化编辑**: 支持拖拽方式对攻略进行增删改操作，可自定义空闲时间段
4. **实时数据集成**: 天气查询、景点预约信息、门票价格等
5. **攻略管理**: 用户可查看、编辑、删除已保存的攻略
6. **学习优化**: 基于用户历史攻略不断优化AI推荐质量

### 2.3 用户角色
- **普通用户**: 注册登录、生成攻略、编辑攻略、查看攻略
- **管理员**: 用户管理、内容审核、系统配置、数据统计

## 3. 系统架构设计

### 3.1 技术架构

```
┌─────────────────────────────────────────────────────┐
│                  前端展示层 (Frontend)                 │
│         HTML/CSS/JavaScript + 拖拽编辑组件            │
└─────────────────────────────────────────────────────┘
                        ↓ HTTP/HTTPS
┌─────────────────────────────────────────────────────┐
│                应用服务层 (Backend)                    │
│              Java Web (Spring Boot)                   │
│  ┌──────────┬──────────┬──────────┬─────────────┐   │
│  │ 用户模块  │ 攻略模块  │ AI服务模块│ 数据集成模块 │   │
│  ├──────────┼──────────┼──────────┼─────────────┤   │
│  │偏好分析模块│预算优化模块│助手模块  │             │   │
│  └──────────┴──────────┴──────────┴─────────────┘   │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│                  数据层 (Data Layer)                   │
│  ┌──────────┬──────────┬──────────┬─────────────┐   │
│  │  MySQL   │  Redis   │   MQ     │ 文件存储    │   │
│  └──────────┴──────────┴──────────┴─────────────┘   │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│                外部服务层 (External)                   │
│  ┌──────────┬──────────┬──────────┬─────────────┐   │
│  │通义千问API│ 天气API  │地图交通API│景点信息API  │   │
│  └──────────┴──────────┴──────────┴─────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 3.2 架构模式
采用经典的**三层架构**:
- **表现层**: 前端页面，负责用户交互和展示
- **业务逻辑层**: Spring Boot后端，处理业务逻辑
- **数据访问层**: MyBatis/JPA，负责数据持久化

### 3.3 设计原则
- **前后端分离**: RESTful API通信
- **微服务预留**: 模块化设计，便于后续拆分
- **高内聚低耦合**: 各模块职责清晰
- **可扩展性**: 支持横向扩展

## 4. 模块设计

### 4.1 模块划分

#### 4.1.1 用户管理模块 (User Module)
**职责**: 
- 用户注册、登录、注销
- 用户信息管理
- 权限控制

**关键技术**:
- JWT Token认证
- Session管理(Redis)
- 密码加密(BCrypt)

#### 4.1.2 攻略管理模块 (Itinerary Module)
**职责**:
- 攻略创建、查询、更新、删除
- 攻略版本管理
- 攻略分享功能

**核心功能**:
- 攻略基本信息管理
- 攻略详细内容管理(日程安排)
- 攻略标签分类
- 用户自定义编辑保存
- **空闲时间管理**: 支持标记时间段为自由活动

#### 4.1.3 智能偏好分析模块 (Preference Analyzer Module) ⭐新增
**职责**:
- 解析用户自然语言输入
- 提取关键词并映射到8个维度
- 计算各维度权重(0-100分)
- 基于权重生成预算分配建议

**核心维度**:
- 美食指数、文化深度、自然风光、休闲程度
- 亲子友好、摄影需求、购物倾向、探险精神

**工作流程**:
1. 接收用户自由文本输入
2. NLP语义分析提取关键词
3. 规则映射计算原始权重
4. AI二次校准优化权重
5. 返回权重分布和预算建议

#### 4.1.4 AI服务模块 (AI Service Module)
**职责**:
- 与通义千问API交互
- Prompt工程化管理
- AI响应解析与格式化

**工作流程**:
1. 收集用户输入参数和偏好权重
2. 构建增强版Prompt(包含权重信息和预算分配)
3. 调用通义千问API
4. 解析JSON格式的AI响应
5. 转换为HTML格式

#### 4.1.5 数据集成模块 (Data Integration Module)
**职责**:
- 天气数据查询
- 景点信息查询
- 交通路线规划
- 门票价格查询

**外部API**:
- 高德地图/百度地图API (交通、位置)
- 和风天气/心知天气API (天气预报)
- 携程/美团API (景点门票、美食)

#### 4.1.6 缓存模块 (Cache Module)
**职责**:
- 热点数据缓存
- Session存储
- 临时数据缓存

**缓存策略**:
- 天气数据: 缓存1小时
- 景点信息: 缓存24小时
- 用户Session: 30分钟过期
- 攻略模板: 长期缓存

#### 4.1.7 消息队列模块 (MQ Module)
**职责**:
- 异步任务处理
- 日志记录
- 邮件通知

**应用场景**:
- 攻略生成异步处理
- 用户操作日志
- 系统监控告警

### 4.2 模块依赖关系

```
用户管理模块 ← 所有模块
攻略管理模块 → 偏好分析模块
攻略管理模块 → AI服务模块
攻略管理模块 → 数据集成模块
偏好分析模块 → AI服务模块 (用于语义校准)
AI服务模块 → 数据集成模块 (获取实时数据)
所有模块 → 缓存模块
所有模块 → 消息队列模块
```

## 5. 数据流设计

### 5.1 智能偏好分析流程

```
用户输入自然语言描述 → 前端发送请求 → 偏好分析模块接收
    ↓
文本预处理(去标点、转小写) → 关键词提取 → 停用词过滤
    ↓
规则映射计算原始权重 → 归一化处理(0-100)
    ↓
调用AI进行语义校准 → 返回最终权重分布
    ↓
基于权重生成预算分配建议 → 返回前端展示
```

### 5.2 攻略生成流程(新版)

```
用户输入目的地+天数+预算+自然语言描述 → 前端验证 → 后端接收
    ↓
调用偏好分析模块 → 获取8维度权重 → 生成预算分配方案
    ↓
查询缓存(如有) → 调用数据集成API(天气、景点等)
    ↓
构建增强版Prompt(包含权重+预算) → 调用通义千问API → 解析响应
    ↓
生成HTML攻略 → 返回前端展示 → 用户编辑
    ↓
用户确认保存 → 存储数据库 → 加入缓存 → 异步记录日志
```

### 5.3 空闲时间管理流程

```
用户在编辑器中选择时间段 → 标记为"空闲时间"
    ↓
设置空闲类型: [自由活动 | 休息调整 | 机动时间]
    ↓
可选填写备注说明 → 保存到攻略数据结构
    ↓
前端渲染时显示特殊样式 → 后端存储时保留标记
```

### 5.4 数据流向

```
前端请求 → Controller → Service → Mapper → Database
                ↓           ↓
            Cache        External APIs
                ↓
            MQ (异步任务)
```

## 6. 接口设计原则

### 6.1 RESTful API规范
- GET: 查询资源
- POST: 创建资源
- PUT: 更新资源
- DELETE: 删除资源

### 6.2 统一响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 6.3 错误处理
- 400: 请求参数错误
- 401: 未授权
- 403: 禁止访问
- 404: 资源不存在
- 500: 服务器内部错误

## 7. 安全设计

### 7.1 认证授权
- JWT Token实现无状态认证
- Refresh Token机制
- 基于角色的访问控制(RBAC)

### 7.2 数据安全
- 密码加密存储(BCrypt)
- HTTPS传输加密
- SQL注入防护(MyBatis预编译)
- XSS攻击防护

### 7.3 接口安全
- API限流(Redis实现)
- 签名验证
- CORS跨域配置

## 8. 性能设计

### 8.1 缓存策略
- 多级缓存: 浏览器缓存 → CDN → Redis → 本地缓存
- 缓存预热: 热门景点、天气数据
- 缓存穿透保护: 布隆过滤器

### 8.2 数据库优化
- 读写分离
- 索引优化
- 分库分表预留(按用户ID哈希)

### 8.3 异步处理
- 耗时操作异步化(AI调用、日志记录)
- 线程池管理
- 消息队列削峰填谷

## 9. 部署架构

### 9.1 部署方案

```
                    ┌─────────────┐
                    │   Nginx     │
                    │ 负载均衡     │
                    └─────────────┘
                           ↓
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                  ↓
  ┌──────────┐      ┌──────────┐      ┌──────────┐
  │ App节点1 │      │ App节点2 │      │ App节点N │
  └──────────┘      └──────────┘      └──────────┘
        ↓                  ↓                  ↓
  ┌────────────────────────────────────────────────┐
  │          Redis Cluster + MySQL主从              │
  └────────────────────────────────────────────────┘
        ↓
  ┌────────────────────────────────────────────────┐
  │              外部API服务                         │
  └────────────────────────────────────────────────┘
```

### 9.2 环境配置
- **开发环境**: 单机部署，便于调试
- **测试环境**: 模拟生产环境配置
- **生产环境**: 集群部署，高可用

## 10. 技术选型

### 10.1 后端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| Java | 11.0.16+ | 编程语言 |
| Spring Boot | 2.7.18 | Web框架 |
| Spring Security | 5.x | 安全框架 |
| MyBatis-Plus | 3.5.5 | ORM框架 |
| MySQL | 8.0 | 关系数据库 |
| Redis | 5.0.14 | 缓存数据库 |
| RabbitMQ | 4.2.3 | 消息队列 |
| Maven | 3.9.10 | 项目管理 |

### 10.2 前端技术栈
| 技术 | 版本 | 说明 |
|------|------|------|
| HTML5/CSS3 | - | 基础结构 |
| JavaScript | ES6+ | 交互逻辑 |
| Vue.js/React | 最新版(可选) | UI框架 |
| SortableJS | 最新版 | 拖拽组件 |

### 10.3 第三方服务
| 服务 | 提供商 | 用途 |
|------|--------|------|
| AI大模型 | 阿里云通义千问 | 攻略生成、语义分析 |
| 天气API | 和风天气/心知天气 | 天气预报 |
| 地图API | 高德地图/百度地图 | 交通路线、位置 |
| 景点API | 携程/美团(可选) | 门票、景点信息 |

## 11. 关键技术方案

### 11.1 智能偏好分析方案 ⭐核心创新
**核心思路**: NLP语义分析 + 规则映射 + AI校准

**实施步骤**:
1. 定义8个核心维度及其权重范围(0-100)
2. 建立关键词到维度的映射规则库
3. 实现文本预处理和关键词提取算法
4. 调用通义千问进行语义理解和权重校准
5. 基于权重动态计算预算分配比例
6. 将权重信息注入到攻略生成Prompt中

### 11.2 AI攻略生成方案(增强版)
**核心思路**: 结构化Prompt + 权重注入 + 模板引擎

**实施步骤**:
1. 定义标准化的用户输入参数结构
2. 接收偏好分析模块返回的8维度权重
3. 基于权重生成个性化预算分配方案
4. 构建增强版Prompt(包含权重+预算+具体要求)
5. 调用通义千问API，设置合适的temperature和max_tokens
6. 解析JSON格式的AI响应
7. 渲染为HTML模板

### 11.3 空闲时间管理方案 ⭐新增
**核心技术**: ContentEditable + 自定义数据结构

**功能实现**:
- 在日程编辑器中添加"标记为空闲"按钮
- 空闲时间类型: 自由活动/休息调整/机动时间
- 支持填写备注说明
- 前端渲染时使用特殊样式区分
- 后端存储时保留空闲标记和类型

### 11.4 HTML攻略编辑方案
**核心技术**: ContentEditable + SortableJS

**功能实现**:
- 使用ContentEditable实现内容编辑
- SortableJS实现拖拽排序
- 定期自动保存草稿
- 版本对比与回滚
- **支持插入空闲时间段**

### 11.5 实时数据集成方案
**策略**: 同步+异步混合模式

**实施方式**:
- 攻略生成时同步调用天气API获取预报
- 异步调用景点API补充详细信息
- 缓存策略减少重复调用
- 降级方案: API失败时使用默认值

## 12. 监控与日志

### 12.1 日志体系
- **应用日志**: Logback记录业务日志
- **访问日志**: Nginx access log
- **错误日志**: 单独error.log文件
- **审计日志**: 用户关键操作记录

### 12.2 监控指标
- QPS/TPS
- 响应时间(RT)
- 错误率
- CPU/内存使用率
- Redis命中率
- MQ堆积情况

## 13. 扩展性设计

### 13.1 功能扩展点
- 支持多语言攻略生成
- 增加攻略社区分享功能
- 添加行程预算统计
- 集成在线预订功能
- **扩展更多偏好维度**(如:夜生活、购物深度等)

### 13.2 技术扩展点
- AI模型可替换(支持多模型切换)
- 数据库可水平扩展(分库分表)
- 服务可微服务化拆分

## 14. 风险评估与应对

### 14.1 技术风险
| 风险 | 影响 | 应对措施 |
|------|------|----------|
| AI API不稳定 | 攻略生成失败 | 重试机制、降级方案、多模型备份 |
| 并发量大 | 系统响应慢 | 限流、缓存、异步处理 |
| 数据一致性 | 缓存与DB不一致 | 定期同步、最终一致性 |
| 偏好分析不准确 | 用户体验差 | 人工反馈机制、持续优化算法 |

### 14.2 业务风险
| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 用户隐私泄露 | 法律风险 | 数据加密、权限控制 |
| AI生成内容不当 | 用户体验差 | 内容过滤、人工审核 |

## 15. 附录

### 15.1 术语表
- **HLD**: High Level Design，概要设计
- **LLD**: Low Level Design，详细设计
- **API**: Application Programming Interface，应用程序接口
- **JWT**: JSON Web Token，一种认证机制
- **RBAC**: Role-Based Access Control，基于角色的访问控制
- **NLP**: Natural Language Processing，自然语言处理

### 15.2 参考文档
- Spring Boot官方文档
- 通义千问API文档
- MySQL官方文档
- Redis官方文档
